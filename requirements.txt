Django==1.5.1
pytz==2013b             # Not "required", but should always be included when dealing with time

# Template processing
Coffin==0.3.7           # Makes Jinja2 integration into django pretty seamless
Jinja2==2.6             # Higher performance and generally better template system
MarkupSafe==0.18        # Makes Jinja2 escaping faster

django-suit==0.2.2      # Much better looking django admin

# Asset handling
webassets==0.8
django-assets==0.8

# Static file handling
dj-static==0.0.5        # WSGI app for easily serving statics in a Heroku friendly and more efficient way than django default static
django-storages==1.1.8  # Use me + boto for high traffic applications
boto==2.9.3             # Requires a S3 bucket and proper AWS config. More work, but higher performance

python-memcached==1.51  # Python implementation of memcached interface
hash-ring==1.3.1        # Consistent hashing for MemcachedCache

django-debug-toolbar==0.9.4
ujson==1.33
yuicompressor==2.4.7

# Server Stuff
gunicorn==0.17.4
greenlet==0.4.1
./ext/gevent-1.0rc2.tar.gz

# CMGS fork of PyMySQL which fixes django 1.5 compat issue
./ext/pymysql-cmgs.zip
